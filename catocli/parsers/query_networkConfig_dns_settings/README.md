
## CATO-CLI - query.networkConfig.dns.settings:
[Click here](https://api.catonetworks.com/documentation/#query-query.networkConfig.dns.settings) for documentation on this operation.

### Usage for query.networkConfig.dns.settings:

```bash
catocli query networkConfig dns settings -h

catocli query networkConfig dns settings <json>

catocli query networkConfig dns settings --json-file query.networkConfig.dns.settings.json

catocli query networkConfig dns settings '{"accountId":"id"}'

catocli query networkConfig dns settings '{
    "accountId": "id"
}'
```

#### Operation Arguments for query.networkConfig.dns.settings ####

`accountId` [ID] - (required) N/A    
