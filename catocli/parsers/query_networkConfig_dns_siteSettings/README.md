
## CATO-CLI - query.networkConfig.dns.siteSettings:
[Click here](https://api.catonetworks.com/documentation/#query-query.networkConfig.dns.siteSettings) for documentation on this operation.

### Usage for query.networkConfig.dns.siteSettings:

```bash
catocli query networkConfig dns siteSettings -h

catocli query networkConfig dns siteSettings <json>

catocli query networkConfig dns siteSettings --json-file query.networkConfig.dns.siteSettings.json

catocli query networkConfig dns siteSettings '{"accountId":"id","siteRefInput":{"by":"ID","input":"string"}}'

catocli query networkConfig dns siteSettings '{
    "accountId": "id",
    "siteRefInput": {
        "by": "ID",
        "input": "string"
    }
}'
```

#### Operation Arguments for query.networkConfig.dns.siteSettings ####

`accountId` [ID] - (required) N/A    
`siteRefInput` [SiteRefInput] - (required) N/A    
