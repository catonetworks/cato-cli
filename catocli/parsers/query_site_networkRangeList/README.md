
## CATO-CLI - query.site.networkRangeList:
[Click here](https://api.catonetworks.com/documentation/#query-query.site.networkRangeList) for documentation on this operation.

### Usage for query.site.networkRangeList:

```bash
catocli query site networkRangeList -h

catocli query site networkRangeList <json>

catocli query site networkRangeList --json-file query.site.networkRangeList.json

catocli query site networkRangeList '{"networkRangeListInput":{"siteRefInput":{"by":"ID","input":"string"}}}'

catocli query site networkRangeList '{
    "networkRangeListInput": {
        "siteRefInput": {
            "by": "ID",
            "input": "string"
        }
    }
}'
```

#### Operation Arguments for query.site.networkRangeList ####

`accountId` [ID] - (required) N/A    
`networkRangeListInput` [NetworkRangeListInput] - (required) N/A    
