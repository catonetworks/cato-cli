
## CATO-CLI - query.site.siteBackhauling:
[Click here](https://api.catonetworks.com/documentation/#query-query.site.siteBackhauling) for documentation on this operation.

### Usage for query.site.siteBackhauling:

```bash
catocli query site siteBackhauling -h

catocli query site siteBackhauling <json>

catocli query site siteBackhauling --json-file query.site.siteBackhauling.json

catocli query site siteBackhauling '{"siteRefInput":{"by":"ID","input":"string"}}'

catocli query site siteBackhauling '{
    "siteRefInput": {
        "by": "ID",
        "input": "string"
    }
}'
```

#### Operation Arguments for query.site.siteBackhauling ####

`accountId` [ID] - (required) N/A    
`siteRefInput` [SiteRefInput] - (required) N/A    
