
## CATO-CLI - query.site.siteSocketConfiguration:
[Click here](https://api.catonetworks.com/documentation/#query-query.site.siteSocketConfiguration) for documentation on this operation.

### Usage for query.site.siteSocketConfiguration:

```bash
catocli query site siteSocketConfiguration -h

catocli query site siteSocketConfiguration <json>

catocli query site siteSocketConfiguration --json-file query.site.siteSocketConfiguration.json

catocli query site siteSocketConfiguration '{"siteSocketConfigurationInput":{"siteRefInput":{"by":"ID","input":"string"}}}'

catocli query site siteSocketConfiguration '{
    "siteSocketConfigurationInput": {
        "siteRefInput": {
            "by": "ID",
            "input": "string"
        }
    }
}'
```

#### Operation Arguments for query.site.siteSocketConfiguration ####

`accountId` [ID] - (required) N/A    
`siteSocketConfigurationInput` [SiteSocketConfigurationInput] - (required) N/A    
