
## CATO-CLI - query.site.staticHost:
[Click here](https://api.catonetworks.com/documentation/#query-query.site.staticHost) for documentation on this operation.

### Usage for query.site.staticHost:

```bash
catocli query site staticHost -h

catocli query site staticHost <json>

catocli query site staticHost --json-file query.site.staticHost.json

catocli query site staticHost '{"hostId":"id","siteRefInput":{"by":"ID","input":"string"}}'

catocli query site staticHost '{
    "hostId": "id",
    "siteRefInput": {
        "by": "ID",
        "input": "string"
    }
}'
```

#### Operation Arguments for query.site.staticHost ####

`accountId` [ID] - (required) N/A    
`hostId` [ID] - (required) N/A    
`siteRefInput` [SiteRefInput] - (required) N/A    
