{
    "operationName": "sandboxUploadFile",
    "query": "mutation sandboxUploadFile ( $uploadFileInput:UploadFileInput! ) { sandbox ( ) { uploadFile ( input:$uploadFileInput ) { uploadUrl } } }",
    "variables": {
        "uploadFileInput": {
            "fileName": {
                "fileName": "String"
            }
        }
    }
}